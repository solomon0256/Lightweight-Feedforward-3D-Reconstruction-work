# 配置文件参数检查报告

> **检查日期**: 2025-12-30  
> **目的**: 检查配置文件参数设定是否符合K+Q+K→Q实验策略

---

## 📋 检查概述

### 检查的配置文件

1. `expconfigs/distill.yaml` - 蒸馏配置
2. `expconfigs/quant.yaml` - 量化配置
3. `config/workload.yaml` - 工作负载配置（参考）
4. `config/eval_contract.yaml` - 评估合同（已冻结，仅检查）

### 检查原则

- ✅ 参数值是否符合推荐值
- ✅ 参数格式是否正确（列表 vs 单个值）
- ✅ 是否符合实验策略（不做Grid Search）
- ✅ 配置文件间的一致性

---

## 🔍 详细检查结果

### 1. `expconfigs/distill.yaml` 检查

#### 1.1 蒸馏参数

| 参数 | 当前值 | 推荐值 | 状态 | 说明 |
|------|--------|--------|------|------|
| `distill.kd_temperature` | `[3, 5]` | `3.0` 或 `5.0` | ✅ **可接受** | 脚本会取第一个值（3.0），不会做Grid Search |
| `distill.beta_kd` | `[0.5, 0.7]` | `0.5` 或 `0.7` | ✅ **可接受** | 脚本会取第一个值（0.5），不会做Grid Search |
| `distill.gamma_fd` | `[0.0, 0.1]` | `0.0` | ✅ **可接受** | 脚本会取第一个值（0.0），不会做Grid Search |
| `distill.curriculum_pct_task_first` | `0.7` | `0.7` | ✅ **正确** | 课程式蒸馏配置 |

**分析**:
- ✅ 虽然配置文件中是列表格式，但脚本会取第一个值（见`scripts/train_distill.py` lines 243-250）
- ✅ **不会导致Grid Search**，符合实验策略
- ⚠️ 如果想更明确，可以改为单个值，但不是必需的

**建议**: ✅ **保持现状即可**（脚本已正确处理）

---

#### 1.2 Student架构配置

| 参数 | 当前值 | 推荐值 | 状态 | 说明 |
|------|--------|--------|------|------|
| `student.arch` | `"dust3r_student_s"` | `"dust3r_student_s"` | ✅ **正确** | Student架构预设 |
| `student.student_config.encoder_layers` | `10` | `10-16` | ✅ **合理** | 从24层减少到10层（约58%压缩） |
| `student.student_config.mha_heads_ratio` | `0.8` | `0.7-0.8` | ✅ **合理** | 注意力头缩放 |
| `student.student_config.ffn_ratio` | `0.8` | `0.7-0.8` | ✅ **合理** | FFN宽度缩放 |

**分析**:
- ✅ Student架构配置合理
- ✅ 压缩比例符合预期（30-35%压缩）

**建议**: ✅ **保持现状**

---

#### 1.3 训练参数

| 参数 | 当前值 | 推荐值 | 状态 | 说明 |
|------|--------|--------|------|------|
| `optim.lr` | `2e-4` | `1e-4` 或 `2e-4` | ✅ **可接受** | 2e-4略高，但可接受 |
| `data.batch_size` | `1` | `1` | ✅ **正确** | DUSt3R要求BS=1 |
| `run.max_epochs` | `30` | `30` | ✅ **正确** | 标准训练轮数 |
| `run.early_stop_patience` | `3` | `3` | ✅ **正确** | 早停配置 |

**分析**:
- ✅ 训练参数基本正确
- ⚠️ 学习率`2e-4`略高于推荐值`1e-4`，但可接受

**建议**: 
- ✅ **可以保持现状**（2e-4可接受）
- ⚠️ 如果训练不稳定，可以改为`1e-4`

---

#### 1.4 其他参数

| 参数 | 当前值 | 状态 | 说明 |
|------|--------|------|------|
| `run.seed` | `42` | ✅ **正确** | 随机种子 |
| `run.exp_name` | `"K_only_student_v1"` | ✅ **正确** | 实验名称 |
| `paths.*` | 相对路径 | ✅ **正确** | 路径配置 |

---

### 2. `expconfigs/quant.yaml` 检查

#### 2.1 量化参数

| 参数 | 当前值 | 推荐值 | 状态 | 说明 |
|------|--------|--------|------|------|
| `quant.scheme` | `"W8A8"` | `"W8A8"` | ✅ **正确** | INT8量化 |
| `quant.weight.granularity` | `"per-channel"` | `"per-channel"` | ✅ **正确** | 权重per-channel量化 |
| `quant.activation.granularity` | `"per-tensor"` | `"per-tensor"` | ✅ **正确** | 激活per-tensor量化 |
| `quant.weight.symmetric` | `true` | `true` | ✅ **正确** | 对称量化 |
| `quant.activation.symmetric` | `false` | `false` | ✅ **正确** | 非对称量化 |

**分析**:
- ✅ 量化参数配置完全正确
- ✅ 符合标准量化实践

**建议**: ✅ **保持现状**

---

#### 2.2 Keep-list配置

| 参数 | 当前值 | 推荐值 | 状态 | 说明 |
|------|--------|--------|------|------|
| `quant.keep_list_ops` | `[LayerNorm, Softmax, Scale, MatMul_QK, Div_Sqrt_d]` | 标准列表 | ✅ **正确** | 保护关键操作 |
| `quant.keep_list_modules` | `["encoder.*.ln*", "encoder.*.attn.softmax", "decoder.*.ln*"]` | 标准列表 | ✅ **正确** | 保护关键模块 |

**分析**:
- ✅ Keep-list配置完整
- ✅ 包含了所有关键层（LayerNorm, Softmax, Attention）

**建议**: ✅ **保持现状**

---

#### 2.3 校准配置

| 参数 | 当前值 | 推荐值 | 状态 | 说明 |
|------|--------|--------|------|------|
| `quant.calibration.num_images` | `512` | `≥512` | ✅ **正确** | 校准集大小 |
| `quant.calibration.preprocess.resize_short` | `512` | `512` | ✅ **正确** | 输入尺寸 |
| `quant.calibration.preprocess.center_crop` | `[512, 384]` | `[512, 384]` | ✅ **正确** | 裁剪尺寸 |

**分析**:
- ✅ 校准配置正确
- ✅ 输入尺寸与workload.yaml一致

**建议**: ✅ **保持现状**

---

#### 2.4 QAT配置

| 参数 | 当前值 | 推荐值 | 状态 | 说明 |
|------|--------|--------|------|------|
| `quant.qat.lr` | `1e-4` | `1e-4` | ✅ **正确** | QAT学习率 |
| `run.qat_max_epochs` | `15` | `10-15` | ✅ **正确** | QAT训练轮数 |
| `quant.qat.freeze_keep_list` | `true` | `true` | ✅ **正确** | 冻结keep-list层 |

**分析**:
- ✅ QAT配置正确
- ✅ 符合标准QAT实践

**建议**: ✅ **保持现状**

---

#### 2.5 其他参数

| 参数 | 当前值 | 状态 | 说明 |
|------|--------|------|------|
| `run.seed` | `42` | ✅ **正确** | 随机种子 |
| `run.mode` | `"auto"` | ✅ **正确** | 自动模式（PTQ→QAT） |
| `model.weights_fp32` | `"outputs/student_fp32_best.pth"` | ✅ **正确** | K→Q时使用Student模型 |

---

### 3. 配置文件一致性检查

#### 3.1 输入尺寸一致性

| 配置文件 | 参数路径 | 值 | 状态 |
|---------|---------|------|------|
| `workload.yaml` | `input.shape` | `[1, 3, 512, 384]` | ✅ |
| `quant.yaml` | `quant.calibration.preprocess.center_crop` | `[512, 384]` | ✅ |
| `quant.yaml` | `tensorrt.profiles[0].images` | `[1, 3, 512, 384]` | ✅ |
| `eval_contract.yaml` | `input.resolution` | `[512, 384]` | ✅ |

**结论**: ✅ **输入尺寸一致**（都是512x384）

---

#### 3.2 Seed一致性

| 配置文件 | 参数路径 | 值 | 状态 |
|---------|---------|------|------|
| `workload.yaml` | `seed` | `42` | ✅ |
| `distill.yaml` | `run.seed` | `42` | ✅ |
| `quant.yaml` | `run.seed` | `42` | ✅ |
| `eval_contract.yaml` | `data.sampling_seed` | `42` | ✅ |

**结论**: ✅ **Seed一致**（都是42）

---

#### 3.3 路径配置一致性

| 配置文件 | 参数路径 | 值 | 状态 |
|---------|---------|------|------|
| `distill.yaml` | `paths.checkpoints` | `"checkpoints/"` | ✅ |
| `quant.yaml` | `paths.checkpoints` | `"checkpoints/"` | ✅ |
| `distill.yaml` | `paths.outputs` | `"outputs/"` | ✅ |
| `quant.yaml` | `paths.outputs` | `"outputs/"` | ✅ |
| `distill.yaml` | `paths.logs` | `"logs/"` | ✅ |
| `quant.yaml` | `paths.logs` | `"logs/"` | ✅ |

**结论**: ✅ **路径配置一致**

---

## ⚠️ 发现的问题和建议

### 问题1: 学习率略高（非关键）

**位置**: `expconfigs/distill.yaml`

**问题**: `optim.lr: 2e-4` 略高于推荐值 `1e-4`

**影响**: ⚠️ **轻微** - 可能导致训练不稳定，但通常可接受

**建议**: 
- ✅ **可以保持现状**（2e-4可接受）
- ⚠️ 如果训练不稳定，可以改为`1e-4`

**优先级**: ⚠️ 低（可选修改）

---

### 问题2: 参数格式为列表（非问题）

**位置**: `expconfigs/distill.yaml`

**问题**: `kd_temperature`, `beta_kd`, `gamma_fd` 是列表格式 `[3, 5]`

**影响**: ✅ **无影响** - 脚本会取第一个值，不会做Grid Search

**分析**: 
- 脚本已正确处理列表格式（取第一个值）
- 不会导致Grid Search
- 符合实验策略

**建议**: ✅ **保持现状即可**（如果想更明确，可以改为单个值，但不是必需的）

---

## ✅ 检查结论

### 总体评价

✅ **配置文件参数设定正确，符合实验策略**

**检查通过的项目**:
- ✅ 蒸馏参数设定正确
- ✅ 量化参数设定正确
- ✅ Student架构配置合理
- ✅ 训练参数基本正确
- ✅ Keep-list配置完整
- ✅ 校准配置正确
- ✅ QAT配置正确
- ✅ 配置文件间一致性良好

**发现的问题**:
- ⚠️ 学习率略高（非关键，可接受）
- ✅ 参数列表格式（非问题，脚本已正确处理）

---

## 📝 修改建议

### 可选修改（非必需）

#### 修改1: 降低学习率（可选）

**文件**: `expconfigs/distill.yaml`

**修改**:
```yaml
optim:
  lr: 1e-4  # 从 2e-4 改为 1e-4（更保守）
```

**理由**: 更保守的学习率，训练更稳定

**优先级**: ⚠️ 低（可选）

---

#### 修改2: 参数改为单个值（可选）

**文件**: `expconfigs/distill.yaml`

**修改**:
```yaml
distill:
  kd_temperature: 3.0  # 从 [3, 5] 改为 3.0
  beta_kd: 0.5         # 从 [0.5, 0.7] 改为 0.5
  gamma_fd: 0.0        # 从 [0.0, 0.1] 改为 0.0
```

**理由**: 更明确，避免误解

**优先级**: ⚠️ 低（可选，脚本已正确处理）

---

### 必需修改

**无** - 所有必需参数都已正确设定

---

## 🎯 最终建议

### 配置文件状态

✅ **配置文件已准备好，可以直接使用**

**建议**:
1. ✅ **保持现状** - 所有参数都正确，可以直接使用
2. ⚠️ **可选优化** - 如果想更保守，可以降低学习率（非必需）

### 可以开始执行

- ✅ 配置文件参数检查通过
- ✅ 可以开始K-only实验
- ✅ 然后进行Q-only和K→Q实验

---

**最后更新**: 2025-12-30

