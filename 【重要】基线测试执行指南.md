# 【重要】基线测试执行指南

> **目的**：为所有轻量化实验建立对比基准  
> **原则**：一次测全，后续只对比，不重测

---

## 一、基线测试要记录什么？

### 1.1 模型本体（与硬件无关）
- [ ] params_total_M — 总参数量
- [ ] params_encoder_M — Encoder 参数量
- [ ] params_decoder_M — Decoder 参数量
- [ ] flops_G — 计算量
- [ ] macs_G — 乘加操作数
- [ ] size_MB — 模型文件大小

### 1.2 显存与内存
- [ ] vram_peak_GB — 峰值显存 (FP32, BS=1)
- [ ] vram_fp16_GB — FP16 模式峰值显存
- [ ] activation_memory_GB — 激活值内存

### 1.3 推理性能
- [ ] t_pair_p50_ms — 单对图时延 (中位数)
- [ ] t_pair_p95_ms — 单对图时延 (95分位)
- [ ] t_pair_mean_ms — 单对图时延 (平均)
- [ ] t_pair_std_ms — 单对图时延 (标准差)
- [ ] t_scene_s — 整场景耗时
- [ ] pairs_per_sec — 吞吐量

### 1.4 精度指标（DUSt3R 官方）
- [ ] L21 — 3D点欧几里得距离
- [ ] Regr3D — 尺度不变 3D 回归误差
- [ ] confidence — 预测置信度
- [ ] reproj_px — 重投影像素误差

### 1.5 位姿估计
- [ ] pose_error_median — 位姿平移误差中位数
- [ ] angular_error_median — 角度误差中位数
- [ ] acc@0.1m,1° — 准确率
- [ ] acc@0.25m,2° — 准确率
- [ ] acc@0.5m,5° — 准确率
- [ ] acc@5m,10° — 准确率

### 1.6 深度/几何
- [ ] absrel — 相对深度误差
- [ ] sqrel — 平方相对误差
- [ ] rmse — 均方根误差
- [ ] rmse_log — 对数均方根误差
- [ ] delta1/delta2/delta3 — 阈值正确率

### 1.7 点云质量
- [ ] chamfer — Chamfer Distance
- [ ] chamfer_L1 / chamfer_L2
- [ ] f_score — F-Score
- [ ] point_to_plane — 点到平面误差

### 1.8 能耗（可选）
- [ ] gpu_power_avg_W — 平均功耗
- [ ] gpu_power_peak_W — 峰值功耗
- [ ] energy_per_pair_J — 每对图能耗

### 1.9 环境信息
- [ ] GPU 型号、显存、驱动
- [ ] CPU 型号、核心数
- [ ] RAM、OS
- [ ] Python、PyTorch、CUDA、cuDNN 版本
- [ ] commit hash、测试日期

---

## 二、轻量化后要对比什么？

### 2.1 核心对比表（论文必须有）

| 指标 | Baseline | K-only | Q-only | K→Q | 单位 |
|------|----------|--------|--------|-----|------|
| Params | 571.2 | ? | ? | ? | M |
| FLOPs | 123.4 | ? | ? | ? | G |
| Size | 2180 | ? | ? | ? | MB |
| VRAM | 2.77 | ? | ? | ? | GB |
| Latency | 45.2 | ? | ? | ? | ms |
| Speedup | 1.0x | ? | ? | ? | x |
| L21 | 0.025 | ? | ? | ? | - |
| δ1 | 94.5% | ? | ? | ? | % |

### 2.2 对比维度

**精度保持率** = 轻量化后精度 / Baseline 精度 × 100%
**压缩率** = Baseline 大小 / 轻量化后大小
**加速比** = Baseline 时延 / 轻量化后时延

---

## 三、基线测试执行步骤（完整流程）

---

### 🔴 步骤 0：环境准备

```powershell
# 1. 进入项目目录
cd C:\Users\SoLoMoN\Documents\GitHub\Lightweight-Feedforward-3D-Reconstruction-work

# 2. 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 3. 检查 GPU
python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA: {torch.cuda.is_available()}'); print(f'GPU: {torch.cuda.get_device_name(0)}')"
```

**预期输出：**
```
PyTorch: 2.6.0+cu124
CUDA: True
GPU: NVIDIA GeForce RTX 3060 Laptop GPU
```

---

### 🔴 步骤 1：记录环境信息

```powershell
python -c "
import torch
import sys
import platform
import subprocess
import json

env_info = {
    'os': f'{platform.system()} {platform.release()}',
    'python': sys.version.split()[0],
    'pytorch': torch.__version__,
    'cuda': torch.version.cuda,
    'cudnn': torch.backends.cudnn.version(),
    'gpu': torch.cuda.get_device_name(0),
    'gpu_memory_gb': round(torch.cuda.get_device_properties(0).total_memory / 1e9, 2),
}

print('=== 环境信息 ===')
for k, v in env_info.items():
    print(f'  {k}: {v}')

# 保存到文件
with open('logs/env_info.json', 'w') as f:
    json.dump(env_info, f, indent=2)
print('\\n[OK] 已保存到 logs/env_info.json')
"
```

---

### 🔴 步骤 2：本地干跑测试（验证脚本可用）

```powershell
# 先干跑确保脚本没问题！
python scripts/baseline_eval.py --dry-run --num-pairs 5 --output test_run
```

**预期输出：**
```
============================================================
DUSt3R Baseline Evaluation
============================================================
  Exp ID: default
  Device: cuda
  Input shape: (1, 3, 512, 384)
  Dry run: True
============================================================
[INFO] Created dummy model for dry-run
[INFO] Computing model statistics...
...
Evaluation Complete!
```

**干跑成功 → 继续步骤3**
**干跑失败 → 检查错误，修复后重试**

---

### 🔴 步骤 3：下载 DUSt3R 权重（如果没有）

```powershell
# 检查权重是否存在
$weights = "checkpoints/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth"
if (Test-Path $weights) {
    Write-Host "[OK] 权重文件已存在: $weights"
} else {
    Write-Host "[INFO] 开始下载权重..."
    python scripts/download_weights.py
}
```

**或者手动下载：**
- URL: https://download.europe.naverlabs.com/ComputerVision/DUSt3R/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth
- 保存到: `checkpoints/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth`
- 文件大小: 约 2.2GB

---

### 🔴 步骤 4：测量模型本体指标

```powershell
# 用真实模型测量参数量、FLOPs、显存
python scripts/baseline_eval.py \
    --weights checkpoints/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth \
    --dry-run \
    --num-pairs 10 \
    --output baseline_model_info
```

> 注意：即使 `--dry-run`，加载真实权重也能测出真实的参数量和显存

---

### 🔴 步骤 5：准备测试数据集

```powershell
# 检查数据集目录
ls datasets/

# 需要准备的数据集（按需选择）：
# - CO3Dv2 mini（推荐，约 5GB）
# - ScanNet（较大）
# - 自定义图像对
```

**最小测试集准备：**
```powershell
# 创建测试图像对列表
New-Item -ItemType Directory -Force -Path datasets/test_pairs
# 放入几对测试图像，创建 pairs.txt：
# datasets/test_pairs/img1_a.jpg|datasets/test_pairs/img1_b.jpg
# datasets/test_pairs/img2_a.jpg|datasets/test_pairs/img2_b.jpg
```

---

### 🔴 步骤 6：运行完整基线评测

```powershell
# 完整评测（需要数据集）
python scripts/baseline_eval.py \
    --weights checkpoints/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth \
    --exp-config expconfigs/distill.yaml \
    --num-pairs 100 \
    --output teacher_eval
```

**或者继续干跑（无数据集）：**
```powershell
python scripts/baseline_eval.py \
    --weights checkpoints/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth \
    --dry-run \
    --num-pairs 100 \
    --output teacher_eval_dryrun
```

---

### 🔴 步骤 7：验证输出文件

```powershell
# 检查输出
ls logs/
# 应该看到：
# - teacher_eval.json
# - teacher_eval.csv

# 查看日志内容
Get-Content logs/teacher_eval.json | ConvertFrom-Json
```

---

### 🔴 步骤 8：提交结果

```powershell
git add logs/teacher_eval.json logs/teacher_eval.csv logs/env_info.json
git commit -m "baseline: Teacher 模型基线测试结果"
git push
```

---

## 三.5、快速命令汇总

```powershell
# ========== 一键执行（复制粘贴） ==========

# 1. 激活环境
cd C:\Users\SoLoMoN\Documents\GitHub\Lightweight-Feedforward-3D-Reconstruction-work
.\.venv\Scripts\Activate.ps1

# 2. 干跑测试
python scripts/baseline_eval.py --dry-run --num-pairs 5 --output test

# 3. 真实模型评测（无数据）
python scripts/baseline_eval.py --weights checkpoints/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth --dry-run --num-pairs 50 --output teacher_eval

# 4. 真实模型评测（有数据）
python scripts/baseline_eval.py --weights checkpoints/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth --num-pairs 100 --output teacher_eval

# 5. 保存结果
git add logs/ && git commit -m "baseline: 基线测试" && git push
```

---

## 四、baseline_eval.py 实际支持的参数

```
python scripts/baseline_eval.py --help

可用参数：
  --exp-config FILE      实验配置文件 (yaml)
  --dry-run              干跑模式（用虚拟数据）
  --seed N               随机种子
  --device DEV           设备 (cuda/cpu)
  --weights PATH         Teacher 模型权重路径
  --num-pairs N          评测 pair 数量限制
  --output NAME          输出日志文件名
```

### 4.1 脚本执行流程

```
baseline_eval.py 执行流程：

1. 加载配置 → ExperimentConfig
2. 检查设备 → CUDA 可用性
3. 加载模型：
   - 有 --weights → 加载真实 DUSt3R
   - 无 --weights 或 --dry-run → 加载 Dummy 模型
4. 加载数据：
   - 有数据集配置 → 加载真实图像对
   - --dry-run → 生成随机张量
5. 运行评测：
   - 计算模型参数量、FLOPs
   - 测量显存占用
   - 测量推理时延
   - （可选）计算精度指标
6. 保存日志 → logs/{output}.json + .csv
```

### 4.2 依赖的工具模块

| 模块 | 路径 | 功能 |
|------|------|------|
| config.py | scripts/utils/ | 配置管理 |
| logger.py | scripts/utils/ | 日志记录 |
| timer.py | scripts/utils/ | 时延测量 |
| model_stats.py | scripts/utils/ | 模型统计 |
| metrics.py | scripts/utils/ | 质量指标 |

---

## 五、输出 JSON 格式

```json
{
  "exp_id": "baseline_teacher_v1",
  "model": "DUSt3R_ViTLarge_BaseDecoder_512_dpt",
  "datetime": "2025-12-25T10:00:00",
  "commit": "abc1234",
  
  "model_info": {
    "params_total_M": 571.2,
    "params_encoder_M": 304.0,
    "params_decoder_M": 267.2,
    "flops_G": 123.4,
    "size_MB": 2180
  },
  
  "resources": {
    "vram_peak_fp32_GB": 2.77,
    "vram_peak_fp16_GB": 1.5
  },
  
  "latency": {
    "t_pair_p50_ms": 45.2,
    "t_pair_p95_ms": 48.1,
    "t_pair_mean_ms": 45.5,
    "t_pair_std_ms": 2.3,
    "pairs_per_sec": 22.1,
    "warmup_runs": 10,
    "measure_runs": 100
  },
  
  "quality": {
    "L21": 0.025,
    "Regr3D": 0.032,
    "absrel": 0.089,
    "rmse": 0.312,
    "delta1": 0.945,
    "chamfer": 0.018,
    "pose_error_median_m": 0.15,
    "angular_error_median_deg": 2.3,
    "acc_0.25m_2deg": 68.5
  },
  
  "environment": {
    "gpu": "NVIDIA RTX 3060 Laptop GPU",
    "gpu_memory_GB": 6,
    "pytorch": "2.6.0+cu124",
    "cuda": "12.4"
  }
}
```

---

## 六、Checklist（执行前检查）

### 环境检查
- [ ] GPU 可用
- [ ] PyTorch CUDA 版本正确
- [ ] 虚拟环境激活
- [ ] DUSt3R 模型能加载

### 数据检查
- [ ] 测试数据集准备好
- [ ] 数据路径配置正确

### 脚本检查
- [ ] baseline_eval.py 存在
- [ ] 所有依赖安装完成

### 执行检查
- [ ] 无其他 GPU 进程
- [ ] 显存已清理
- [ ] 电脑不会休眠

---

## 七、执行时间估计

| 步骤 | 预计时间 | 备注 |
|------|----------|------|
| 环境准备 | 5 min | |
| 模型加载 | 2 min | 首次下载较久 |
| 显存测量 | 5 min | |
| 推理性能 | 10 min | 100次测量 |
| 精度测试 | 30-60 min | 取决于数据集大小 |
| 能耗测量 | 10 min | 可选 |
| 生成报告 | 2 min | |
| **总计** | **1-1.5 小时** | |

---

## 八、下一步

基线测试完成后：
1. ✅ 保存 `logs/teacher_eval.json`
2. ✅ 保存 `reports/baseline_report.md`
3. ✅ git commit 并 push
4. → 开始 K-only 蒸馏实验
5. → 每个实验结果与 baseline 对比

---

## 九、常见问题

### Q: 本地 6GB 显存够吗？
**A:** 够。DUSt3R FP32 推理峰值约 2.77GB。

### Q: 精度测试需要完整数据集吗？
**A:** 基线测试建议用完整测试集。但本地调试可以先用子集。

### Q: 测试要多久？
**A:** 完整基线测试约 1-1.5 小时。

### Q: 结果保存在哪里？
**A:**
- `logs/teacher_eval.json` — 原始数据
- `reports/baseline_report.md` — 报告
- `reports/baseline_report.csv` — 表格

---

## 十、现在就执行！一步一步跟着做

### 🚀 第一步：打开终端

按 `Ctrl+~` 或在 VS Code 菜单 Terminal → New Terminal

---

### 🚀 第二步：激活环境（复制粘贴）

```powershell
cd C:\Users\SoLoMoN\Documents\GitHub\Lightweight-Feedforward-3D-Reconstruction-work
.\.venv\Scripts\Activate.ps1
```

✅ 看到 `(.venv)` 前缀 → 成功

---

### 🚀 第三步：验证脚本可运行（复制粘贴）

```powershell
python scripts/baseline_eval.py --dry-run --num-pairs 3 --output quick_test
```

✅ 看到 "Evaluation Complete!" → 成功
❌ 报错 → 检查错误信息

---

### 🚀 第四步：真实模型评测

**选项 A：有权重文件**
```powershell
python scripts/baseline_eval.py --weights checkpoints/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth --dry-run --num-pairs 50 --output teacher_eval
```

**选项 B：没有权重文件（先干跑）**
```powershell
python scripts/baseline_eval.py --dry-run --num-pairs 50 --output teacher_eval_mock
```

---

### 🚀 第五步：查看结果

```powershell
Get-Content logs/teacher_eval.json
```

---

### 🚀 第六步：保存到 Git

```powershell
git add logs/
git commit -m "baseline: Teacher 基线测试"
git push
```

---

## 十一、我该从哪步开始？

```
你现在的状态是？
│
├─ 还没激活环境 → 从「第二步」开始
│
├─ 环境已激活，不确定脚本能不能跑 → 从「第三步」开始
│
├─ 脚本能跑，没有权重文件 → 从「第四步选项 B」开始
│
└─ 脚本能跑，有权重文件 → 从「第四步选项 A」开始
```

---

*最后更新: 2025-12-25*
