# 工作日志 - 2025-12-23

## 今日目标
- [x] 手动还原 DUSt3R baseline

## 完成事项

### 1. 项目决策记录
- 创建了 `IDEA/2025-12-23_项目可行性评估.md`
- 记录了关于 IEEE Access 发表价值的讨论
- 最终决策：继续项目，预算约 $2200（服务器 $200 + 版面费 $2000）

### 2. DUSt3R Baseline 完全还原 ✅

#### 环境配置
- PyTorch: 2.9.1+cpu
- Python: 3.13
- 依赖: roma, einops, gradio, trimesh, scipy 等

#### 测试结果
| 指标 | 结果 |
|------|------|
| 模型参数 | 571.2M |
| 模型加载 | 5.2s |
| 推理时间 | 13.47s (CPU) |
| pts3d 输出 | [2, 384, 512, 3] |
| conf 输出 | [2, 384, 512] |
| pts3d 范围 | [-0.302, 0.606] |
| conf 范围 | [1.001, 11.153] |

#### 关键发现
1. CRoCo 的 `models/` 目录缺少 `__init__.py`，导入会失败
2. 解决方案：脚本自动创建该文件
3. sys.path 顺序很重要：dust3r 必须在 croco 前面

### 3. 代码提交

#### 新增文件
- `scripts/test_dust3r_baseline.py` - Baseline 验证脚本
- `scripts/setup_server.sh` - 服务器一键设置
- `scripts/download_weights.py` - 模型下载脚本
- `docs/baseline_restoration_guide.md` - 还原指南

#### Git 操作
- 添加 dust3r 作为 git submodule
- 推送到 GitHub: `e357a04`

### 4. 服务器使用流程

```bash
# 1. 克隆仓库（包含 submodule）
git clone --recursive https://github.com/solomon0256/Lightweight-Feedforward-3D-Reconstruction-work.git
cd Lightweight-Feedforward-3D-Reconstruction-work

# 2. 一键设置（安装依赖 + 下载模型）
bash scripts/setup_server.sh

# 3. 验证 baseline
python scripts/test_dust3r_baseline.py
```

## 下一步计划

### 立即可做（本地）
- [ ] 完善 `baseline_eval.py` 与 DUSt3R 的集成
- [ ] 设计 Student 模型架构
- [ ] 准备蒸馏训练代码

### 需要服务器
- [ ] 租用 A100 GPU 服务器
- [ ] 运行 GPU baseline 测试
- [ ] 准备数据集（ScanNet / CO3D）
- [ ] 开始蒸馏实验

## 时间记录
- 项目讨论：~2h
- Baseline 还原：~2h
- 调试路径问题：~0.5h
- 总计：~4.5h

## 备注
- 本地机器 CPU-only，推理较慢但功能完整
- 模型权重从 HuggingFace 自动下载，约 1.1GB
- 服务器预计推理时间：<1s（A100 GPU）
