# 工作日志 - 2025-12-23

## 今日目标
- [ ] 手动还原 DUSt3R baseline（准备阶段）

## 完成事项

### 1. 项目决策记录
- 创建了 [IDEA/2025-12-23_项目可行性评估.md](../IDEA/2025-12-23_项目可行性评估.md)
- 记录了关于 IEEE Access 发表价值的讨论
- 最终决策：继续项目，预算约 $2200（服务器 $200 + 版面费 $2000）

### 2. Baseline 还原准备
- ✅ 克隆了 DUSt3R 官方仓库到 `third_party/dust3r/`
- ✅ 包含 CRoCo submodule
- ✅ 创建了手动验证脚本 [scripts/test_dust3r_baseline.py](../scripts/test_dust3r_baseline.py)
- ✅ 编写了详细的还原指南 [docs/baseline_restoration_guide.md](../docs/baseline_restoration_guide.md)

### 3. 代码分析
阅读了 DUSt3R 源码，理解了关键流程：

#### 模型加载
```python
from dust3r.model import AsymmetricCroCo3DStereo
model = AsymmetricCroCo3DStereo.from_pretrained("naver/DUSt3R_ViTLarge_BaseDecoder_512_dpt")
```

#### 推理流程
```python
from dust3r.utils.image import load_images
from dust3r.image_pairs import make_pairs
from dust3r.inference import inference

imgs = load_images(filelist, size=512)
pairs = make_pairs(imgs, scene_graph="complete", symmetrize=True)
output = inference(pairs, model, device, batch_size=1)
```

#### 输出格式
- `output['pred1']` / `output['pred2']`：两个视角的预测
- `pred['pts3d']`：3D 点云，形状 `(B, H, W, 3)`
- `pred['conf']`：置信度图，形状 `(B, H, W)`

### 4. 删除了不成熟的自动化脚本
- ❌ 删除 `scripts/setup_dust3r.sh`
- ❌ 删除 `scripts/download_checkpoints.py`
- 原因：应该先手动验证，再写自动化脚本

## 下一步计划

### 云服务器部署（待执行）
1. 租用 A100 服务器
2. 按照 [docs/baseline_restoration_guide.md](../docs/baseline_restoration_guide.md) 手动执行每一步
3. 运行 `scripts/test_dust3r_baseline.py` 验证
4. 记录环境配置和运行结果

### 待验证清单
- [ ] Conda 环境创建
- [ ] PyTorch CUDA 安装
- [ ] DUSt3R 依赖安装
- [ ] 模型权重下载
- [ ] 推理测试
- [ ] 显存/耗时记录

## 时间记录
- 项目讨论：~2h
- 代码分析：~1h
- 文档编写：~0.5h

## 备注
- 本地机器仅用于开发，无法运行实际实验（Python 3.13，无 CUDA）
- 云服务器部署后才能进行真正的验证
- 谨记：不能在没有验证的情况下标记任务完成
